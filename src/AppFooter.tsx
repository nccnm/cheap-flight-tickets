import React from 'react';
import { Stack, Text, Link, FontWeights, SearchBox } from 'office-ui-fabric-react';

export const AppFooter: React.FunctionComponent = () => {
    return (
        <Stack horizontal gap={15} horizontalAlign="center">
            Footer
      </Stack>
    );
};